# Pinned to the exact versions verified working in .venv. See agent/requirements.txt
# for why: `>=` floors let each image rebuild resolve differently and broke deployed
# chat. The same drift applies here — langgraph in particular changes the planning
# workflow's behaviour between majors, so it must not float.
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-dotenv==1.2.2
requests==2.34.2
pandas==3.0.3
pyarrow==24.0.0
yfinance==1.3.0

# LangGraph financial planning workflow (Financial_Planning / Armstrong)
langgraph==1.2.0
langchain==1.3.1
langchain-openai==1.2.1
langchain-community==0.4.1
langchain-core==1.4.0
babel==2.18.0
numpy==2.4.6
python-pptx==1.0.2
matplotlib==3.11.0
