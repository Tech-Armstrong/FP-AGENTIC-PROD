# Pinned to the exact versions verified working in .venv. See agent/requirements.txt
# for why: `>=` floors let each image rebuild resolve differently and broke deployed chat.
fastapi==0.136.1
uvicorn[standard]==0.47.0
python-dotenv==1.2.2
pydantic==2.13.4
python-multipart==0.0.29

# OCR + extraction pipeline
azure-ai-documentintelligence==1.0.2
azure-core==1.41.0
dspy-ai==3.2.1
litellm==1.86.2
